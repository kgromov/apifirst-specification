{
  "name": "acme-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/cli": "2.0.0"
  },
  "private": true,
  "scripts": {
    "start": "redocly preview",
    "build": "redocly bundle -o dist/bundle.yaml",
    "test": "redocly lint"
  }
}
